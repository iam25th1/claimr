Vercel build fix + admin cleanup

The build failure is two files importing isAdminWallet from
lib/admin-auth.ts. That export disappeared when the Phase B admin batch
replaced admin-auth.ts with the new useIsAdmin hook. The fix is in two
parts:

  1. lib/admin-auth.ts                REBUILT (zip overwrites)
     Adds isAdminWallet(addr) back as a compatibility export. Old files
     that still import it will compile. New code keeps using useIsAdmin().

  2. apply-admin-cleanup.js           REMOVES OLD /admin/ FILES
     Deletes the old admin pages from app/admin/, which are obsolete now
     that /hq/c8m4z2/ has taken over. Without this, the old URL still
     works and the security obscurity is undone.

  Files removed by the script (only if present):
     app/admin/layout.tsx
     app/admin/page.tsx
     app/admin/jobs/
     app/admin/verifier/
     app/admin/wallets/
     app/admin/activity/

  Files NOT touched:
     app/admin/seed/    seed script for test data, deliberate keep


Apply

  Move-Item C:\Users\Bright\Downloads\claimr-admin-cleanup.zip C:\Users\Bright\Documents\GitHub\
  cd C:\Users\Bright\Documents\GitHub ; Expand-Archive -Force claimr-admin-cleanup.zip -DestinationPath claimr
  cd C:\Users\Bright\Documents\GitHub\claimr ; node apply-admin-cleanup.js
  git add -A
  git commit -m "admin: cleanup old /admin/ pages + compat shim for isAdminWallet"
  git push https://iam25th1@github.com/iam25th1/claimr.git


Verify after Vercel rebuild

  - /hq/c8m4z2           management overview loads (works)
  - /admin               returns 404 (the old URL is dead, scramble holds)
  - /admin/seed          still works (kept on purpose)
