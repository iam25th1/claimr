Slice 3 of 3 - Creator experience

Six files. All drop-in replacements. No apply script.

  REBUILT (zip overwrites)
    app/dashboard/page.tsx                  home overview composition
    app/dashboard/discover/page.tsx         job discovery composition
    app/dashboard/my-jobs/page.tsx          my-jobs composition
    app/dashboard/wallet/page.tsx           wallet composition
    app/dashboard/earnings/page.tsx         earnings composition
    components/claimr/earnings-chart.tsx    real chain data (was hardcoded)


What changed and why

/dashboard (home)
  - PageHeader with "Welcome back, [name]" reading email or short wallet
  - At a glance section heading above existing StatsCards
  - Featured and Latest job sections each with "See all" -> discover
  - No new components, just proper hierarchy and section headers

/dashboard/discover
  - PageHeader, then the existing DiscoverFeed (search + featured + latest)
  - Discover internals unchanged. They already pull real data via useJobs.

/dashboard/my-jobs
  - PageHeader with new copy
  - MyJobsList unchanged - already had StatePill, EmptyState, motion from
    earlier batches

/dashboard/wallet
  - Sectioned: Address -> Balances -> Recent transactions -> Swap
  - Replaces the old WalletHeader (which had its own h1) with the unified
    PageHeader
  - All child components unchanged

/dashboard/earnings
  - Sectioned: At a glance -> Monthly earnings -> Payment history
  - EarningsChart is rebuilt entirely. Was hardcoded monthlyData =
    [{month: "Dec", amount: 120}, ...]. Now reads real completed-job
    payouts grouped by month, last 6 months, with animated bar growth
    and an empty state for new creators.


Dependencies

  Requires components/claimr/page-header.tsx (added in Slice 2).
  If Slice 2 wasn't pushed, run:
    Move-Item C:\Users\Bright\Downloads\claimr-slice2-project.zip C:\Users\Bright\Documents\GitHub\
    cd C:\Users\Bright\Documents\GitHub ; Expand-Archive -Force claimr-slice2-project.zip -DestinationPath claimr
  first.


Apply

  Move-Item C:\Users\Bright\Downloads\claimr-slice3-creator.zip C:\Users\Bright\Documents\GitHub\
  cd C:\Users\Bright\Documents\GitHub ; Expand-Archive -Force claimr-slice3-creator.zip -DestinationPath claimr
  cd C:\Users\Bright\Documents\GitHub\claimr ; git status

  Should list:
    modified:   app/dashboard/page.tsx
    modified:   app/dashboard/discover/page.tsx
    modified:   app/dashboard/my-jobs/page.tsx
    modified:   app/dashboard/wallet/page.tsx
    modified:   app/dashboard/earnings/page.tsx
    modified:   components/claimr/earnings-chart.tsx

  Commit and push:
    git add -A ; git commit -m "slice 3: creator experience (home, discover, my-jobs, wallet, earnings)" ; git push https://iam25th1@github.com/iam25th1/claimr.git


Next

Mobile pass. Will touch:
  - Sidebars on both project + dashboard (collapse to bottom tab bar < 768px)
  - PageHeader stacks vertically on mobile
  - Tables become cards (already done in project-jobs-table)
  - Long monospace addresses get smart truncation
