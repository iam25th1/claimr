Finish batch - escrow payout animation + LivingBackground + drop build mask
=============================================================================

What this ships
---------------

NEW FILE:
  components/claimr/escrow-payout-flight.tsx   the USDC fly-to-creator overlay

PATCHES:
  app/project/escrow/page.tsx
    - 3 stat counters animate via AnimatedNumber
    - StatePill replaces inline status spans
    - rows layout-animate in/out of an AnimatePresence
    - EscrowPayoutFlight overlay on each row, fires on status 2 -> 3

  app/project/layout.tsx
  app/dashboard/layout.tsx
    - static blur-blob divs replaced with <LivingBackground />

  next.config.mjs
    - ignoreBuildErrors: true removed
    - project already type-checks clean under strict mode (verified before
      this patch was authored)

Idempotent. Self-deletes on success. Stays in place on failure for
inspection.


Apply
-----

1. Zip lands at:
   C:\Users\Bright\Documents\GitHub\claimr-finish.zip

2. Extract over the repo (one new source file + the apply script):

   cd C:\Users\Bright\Documents\GitHub ; Expand-Archive -Force claimr-finish.zip -DestinationPath claimr

   If git status shows nothing after, use File Explorer extract instead.

3. Apply patches from the repo root:

   cd C:\Users\Bright\Documents\GitHub\claimr ; node apply-finish.js


Full local test
---------------

   npm run build

   Should succeed without the ignoreBuildErrors safety net. If a type error
   surfaces, it was already there and previously hidden. Paste it and we
   fix it.

   npm run dev

   - / and /project           LivingBackground field is rendering. Move
                              the cursor, the field shifts slightly.
                              Switch tabs and back, animation pauses then
                              resumes.

   - /project                 escrow hero number breathes (3.6s scale
                              loop) and counts up on first paint.

   - /project/escrow          three stat counters animate. Rows have
                              StatePill. To test the payout flight
                              without a real chain event, open devtools
                              console and dispatch:

                              window.dispatchEvent(new CustomEvent('claimr:replay-payout', { detail: { jobId: 1 } }))

                              (replace 1 with an actual job id). A +X USDC
                              coin flies from the right side of that row
                              to the left, with a green ripple landing at
                              the creator side.

   - /project/jobs            StatePill on each card, smooth row entrance
   - /dashboard               LivingBackground rendering, all motion
                              wiring from the earlier batch active
   - /dashboard/my-jobs       StatePill near each title


Commit + push
-------------

   cd C:\Users\Bright\Documents\GitHub\claimr ; git add -A ; git commit -m "batch 1 finish: payout flight, living background, strict build" ; git push https://iam25th1@github.com/iam25th1/claimr.git
